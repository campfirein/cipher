*-debug.log
*-error.log
**/.DS_Store
/.idea
/dist
/tmp
/node_modules
oclif.manifest.json



yarn.lock
pnpm-lock.yaml

.npmrc

# ByteRover workspace (local development only)
.brv/

tsconfig.tsbuildinfo

# Temporary markdowns generated by agents for Linear-related work
linear-tmp

# Other temporary agents' artifacts
agent-tmp

# Environment files (dotenv)
.env.development
.env.production
!.env.example

# ByteRover's sub-agent for Claude Code
.claude/agents/byterover.md

# ByteRover's MCP config for Claude Code
.mcp.json

# ByteRover's skills for Claude Code
.claude/skills/byterover
.claude/skills/byterover-onboarding